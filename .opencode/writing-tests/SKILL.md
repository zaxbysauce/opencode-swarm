---
name: writing-tests
description: Compatibility pointer to the canonical opencode-swarm writing-tests skill.
---

# Writing Tests

This path is retained only for older references. Use the canonical skill at:

```text
.opencode/skills/writing-tests/SKILL.md
```

The canonical skill contains the current `bun:test` rules, `test_runner` broad-scope restriction, mock-isolation guidance, CI pipeline structure, and cross-platform test commands.
