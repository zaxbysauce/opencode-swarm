# v6.26.0 — Session Durability & Turbo Mode

## Overview
Implements the v6.26 hotfix plan covering session durability, evidence task
resolution, and a new Turbo Mode for accelerated task delegation.

## Phase 1: Evidence Task Resolution
- Directory-based evidence writes in `delegation-gate.ts`
- Task ID recovery from `plan.json` for cold/resumed sessions
- Targeted regression tests for evidence path stability

## Phase 2: Session Rehydration
- New `rehydrateSessionFromDisk` helper for cold session recovery
- Thread directory propagated through all session bootstrap paths
- Fixed stale-delegation race condition
- Tightened test-engineer scope boundaries
- Regression tests covering session restart scenarios

## Phase 3: Turbo Mode Controls
- `turboMode` flag added to `AgentSessionState`
- New `/swarm turbo` command handler with CLI registration
- Gate bypass for non-Tier 3 tasks when turbo is active
- Evidence tagging and Architect prompt banner for turbo context
- Status indicator surfaced for turbo mode
- 20 comprehensive regression tests

## Testing
- 60 files covered with full QA gates (lint, pre_check_batch, reviewer, test_engineer)
- 100+ tests passing across all surfaces
