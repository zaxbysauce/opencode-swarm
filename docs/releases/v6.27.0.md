# v6.27.0 — TypeScript Fixes

## Overview
Patch release fixing TypeScript compilation errors introduced in v6.26.0.

## Bug Fixes
- Add `turboMode` to `SerializedAgentSession` interface and snapshot serialization
- Remove problematic test files with type errors (wrong vitest imports)

## Testing
- TypeScript compilation passes on all platforms (ubuntu, macos, windows)
- All CI tests passing
