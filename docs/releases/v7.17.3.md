# v7.17.3

## What changed

### Lean Turbo command visibility (registry metadata)
- Updated the `/swarm turbo` command registry entry (`src/commands/registry.ts`) to document lean and standard strategies in the description, args (`on, off, lean, standard, status`), and details fields
- Added `turbo standard off` subcommand to registry and docs (was supported at runtime but undocumented)
- Updated `docs/commands.md` to describe both standard and lean turbo strategies

### Pre-existing turbo test failures fixed
- Added `_internals` DI seam to `src/commands/turbo.ts` for `loadPluginConfigWithMeta`, following the AGENTS.md §7 pattern (established in `gitignore-warning.ts` and `diff-scope.ts`)
- Fixed 5 pre-existing test failures across 3 test files (`turbo.test.ts`, `turbo-registration.test.ts`, `turbo.regression.test.ts`) by mocking `_internals.loadPluginConfigWithMeta` to return standard strategy config in `beforeEach`/`afterEach` pairs
- Added `turbo.internals.test.ts` — 7 tests verifying the `_internals` DI seam (swap, restore, isolation)

## Why

PR #820 added Lean Turbo parallel lane execution but didn't update the command registry metadata or existing turbo tests. This caused:
1. Architects couldn't discover lean turbo subcommands from the registry description/details
2. 5 tests failed because `handleTurboCommand('on')` read the project's lean config and returned "Lean Turbo enabled..." instead of expected "Turbo Mode enabled"

## Migration steps

No migration needed. No API or config changes.

### Test suite quality remediation (6 phases, 25 tasks)

**Phase 1 — Dead code cleanup:**
- Deleted `architect-execution-defaults.test.ts` (0 assertions, entirely skipped)
- Removed ~52 permanent `test.skip` blocks across 20+ files
- Renamed `logger.adversarial.test.ts` → `logger.edge-case.test.ts` (null/undefined inputs, not adversarial)
- Renamed `events.adversarial.test.ts` → `events.type-safety.test.ts` (type narrowing, not adversarial)
- Removed trivial constant/array verification tests from `test-runner.test.ts` and `system-enhancer-task2-2.test.ts`

**Phase 2 — Strengthen weak assertions:**
- Replaced 6 `expect.any(Object)` with `expect.objectContaining()` in `phase-complete-curator.test.ts`
- Replaced 2 standalone `toBeDefined()` with `toMatchObject()` in `curator.adversarial.test.ts`
- Rewrote `pkg-audit-adversarial.test.ts` with 46 genuine adversarial tests (path traversal, injection, malformed input)

**Phase 3 — Deduplication and consolidation:**
- Consolidated 7 `system-enhancer-task2-*` files → 2 test files + 1 shared helper (78 tests preserved)
- Deduplicated `curator.adversarial.test.ts` from 2956→896 lines (69.7% reduction, 99 tests preserved)
- Created `tests/TAXONOMY.md` with 7 test categories (adversarial, edge-case, structural, behavioral, di-seam, type-safety, integration)
- Updated `.github/workflows/ci.yml` for renames/deletions

**Phase 4 — Major rewrites:**
- Rewrote `mutation-test.adversarial.test.ts` (1545→861 lines) — 38 adversarial tests with real `_internals` DI, isolated temp dirs
- Reduced `architect-v6-prompt.test.ts` from 2274→323 lines — 41 structural validation tests
- Removed trivial tests from `lean-turbo-run-phase.test.ts` and `test-runner.test.ts`

**Phase 5 — Coverage gap fill:**
- Created `sme.test.ts` (28 tests), `evidence-service.test.ts` (22 tests), `plan-service.test.ts` (15 tests), `curator-agent.test.ts` (35 tests)
- Added `_internals` DI seams to `src/services/evidence-service.ts` and `src/services/plan-service.ts`
- Expanded `critic.test.ts` with 4 edge case tests (52 total)

**Phase 6 — Fix vi.mock() anti-pattern:**
- Converted `evidence-service-load-evidence.test.ts` (17 tests) from vitest `vi.mock()` to `_internals` DI
- Converted `evidence-service-load-evidence-adversarial.test.ts` (53 tests) from vitest `vi.mock()` to `_internals` DI
- Both files now use `bun:test` exclusively — zero vitest imports remain

**Total impact:** ~335+ tests modified/created, ~4500 lines of dead/weak code removed, 0 production behavior changes

## Known caveats

- The `_internals` DI seam pattern is intentional per AGENTS.md §7 — `mock.module` leaks across files in Bun's shared test-runner process
- Pre-existing lint warnings in `src/turbo/lean/` code (unused imports, explicit any) are unrelated to this change
- Pre-existing parse errors in `guardrails-task23.adversarial.test.ts` and `knowledge-migrator.external.test.ts` are outside this remediation scope
- 6 pre-existing lint errors in non-remediated files remain
