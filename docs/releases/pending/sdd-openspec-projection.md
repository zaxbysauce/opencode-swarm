## Features

- Add OpenSpec-compatible SDD support with `/swarm sdd status`, `/swarm sdd validate`, and `/swarm sdd project`, plus effective-spec projection from `openspec/specs/**/spec.md` and active `openspec/changes/*/specs/**/spec.md` into Swarm planning, drift, lint, and coverage flows.
