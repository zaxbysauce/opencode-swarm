---
"opencode-swarm": patch
---

Capture web/API research as separate evidence documents under `.swarm/evidence-cache/` and require curator-applied memory promotions to remain concise durable facts instead of raw docs or search-result blobs.
